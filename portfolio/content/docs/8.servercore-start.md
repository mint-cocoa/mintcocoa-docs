# 초기 저장소와 ServerCore 전환

이력의 시작점은 단일 포트폴리오 저장소였습니다. 최초 커밋 `85c8f49`는 ServerCore만 담고 있지 않았고, 서버 런타임, 클라이언트, 그래픽스 설계 문서를 함께 포함했습니다.

| 날짜 | 커밋 | 근거 |
| --- | --- | --- |
| 2026-04-09 | `85c8f49 Initial commit: Game server portfolio documents` | ServerCore v4, SwarmDominion DX11/DX12 SVO 설계, FactionClash DX11 클라이언트 자료를 한 번에 추가 |
| 2026-04-13 | `4469271 Add GitHub Pages site with rendered portfolio documents` | 문서를 단순 로컬 파일이 아니라 공개 가능한 Pages 산출물로 전환 |
| 2026-04-14 | `13f2411 Remove non-Pages files from tracking, keep only docs/ for GitHub Pages` | 초기 원본 폴더를 삭제하고 Pages용 산출물 중심으로 저장소 정리 |
| 2026-04-18 | `2e9c0d7 Update portfolio repo links for split server projects` | 서버 프로젝트를 분리된 저장소 단위로 보여주도록 링크 정리 |

## 최초 저장소 인벤토리

초기 `README.md`는 저장소를 다음 세 영역으로 설명했습니다.

| 영역 | 초기 경로 | 성격 | 확인 명령 |
| --- | --- | --- | --- |
| ServerCore v4 | `01_ServerCore_v4/` | C++20, Linux, io_uring 기반 게임 서버 프레임워크와 벤치마크 | `git show 85c8f49:01_ServerCore_v4/ServerCorePortfolio.qmd` |
| SwarmDominion | `02_SwarmDominion_DX11/` | DX11 프로젝트를 DX12 + SVO 레이마칭 렌더러로 확장하기 위한 조사/설계 문서 | `git show 85c8f49:02_SwarmDominion_DX11/SwarmDominion_DX11_Architecture.md` |
| FactionClash Client | `03_FactionClash_Client/` | DirectX 11 아이소메트릭 멀티플레이 던전 RPG 클라이언트 문서와 일부 C++ 소스 | `git show 85c8f49:03_FactionClash_Client/ClientPortfolio.qmd` |

초기 저장소에는 `03_FactionClash_Client/src/*.cpp`, `proto/battle.proto`, `battle.pb.cc`, `battle.pb.h`도 포함되어 있었습니다. 따라서 클라이언트 쪽은 문서만 있던 것이 아니라 일부 구현 파일도 최초 커밋에 들어 있었습니다.

## ServerCore에서 증명되는 내용

초기 ServerCore 문서는 “게임 서버 라이브러리”로 출발합니다. 핵심 근거는 다음입니다.

| 근거 | 내용 |
| --- | --- |
| `io_uring` 직접 구현 이유 | epoll의 read/write syscall 경로와 비교해 SQ/CQ 공유 메모리, Multishot Accept/Recv의 이점을 검증하려고 했습니다. |
| 라이브러리 분리 이유 | `IoRing`, `Session`, `BufferPool` 같은 코어와 `GameRoom`, `PacketSession` 같은 게임 프레임워크 계층이 섞인 문제를 분리했습니다. |
| 주요 기술 | Multishot Accept, Multishot Recv + Provided Buffer, MSG_RING, weak_ptr 기반 IoEvent owner, keep_alive 배치 보호, pending_io_ 기반 세션 수명관리 |
| 벤치마크 | Echo 327K echo/s, 200봇 broadcast p50에서 epoll 대비 큰 지연 차이, Room sharding 효과 |

초기 벤치마크 보고서에는 Integrated, Separated, Epoll 세 모델 비교가 들어 있습니다. 즉 단순 구현 소개가 아니라 같은 게임 로직을 서로 다른 I/O 구조로 비교한 결과가 남아 있습니다.

## FactionClash 클라이언트에서 증명되는 내용

초기 FactionClash 문서는 ServerCore 기반 GameServer와 연동되는 DirectX 11 클라이언트를 설명합니다.

| 근거 | 내용 |
| --- | --- |
| 기술 스택 | C++, DirectX 11, Protobuf 3, Dear ImGui, WinSock2 |
| 네트워크 | `TcpClient`, `PacketFramer`, `PacketHandler`, `PacketBuilder`, Binary Header + Protobuf payload |
| 씬 구조 | LoginScene, CharSelectScene, LobbyScene, GameScene |
| 렌더링 | `InstanceRenderer`, StructuredBuffer 인스턴싱, DrawIndexedInstanced, 프로시저럴 이펙트 셰이더 |
| 서버 연동 | `C_SCENE_READY`, Room broadcast 준비 상태, MapData 기반 dungeon build, 서버 권위 판정 |

이 부분은 현재 저장소에서 삭제되었지만, 최초 커밋에는 `03_FactionClash_Client/proto/battle.proto`와 C++ 소스 파일이 함께 들어 있었으므로 “문서만 작성했다”보다 강한 증거로 볼 수 있습니다.

## SwarmDominion에서 증명되는 내용

SwarmDominion은 구현 완료 프로젝트라기보다 DirectX 학습과 DX12/SVO 확장 설계 자료로 보는 것이 정확합니다.

| 근거 | 내용 |
| --- | --- |
| DX11 구조 학습 | WinMain, D3D11CreateDeviceAndSwapChain, RTV/DSV, ImGui DX11 backend, terrain/unit/particle layer |
| GPU 렌더링 개념 | StructuredBuffer, ShaderResourceView, instanced draw, particle billboard |
| DX12/SVO 확장 설계 | Sparse Voxel Octree, GPU voxelization, traversal shader, chunk dirty update, partial rebuild |
| 레퍼런스 분석 | SparseVoxelOctree, voxel-rs, unity-svo-demo, voxel-raycaster, Teardown, Atomontage, CryEngine SVOGI |

따라서 이력상 SwarmDominion은 “그래픽스 구현 능력”보다는 “렌더링 파이프라인을 학습하고 DX12/SVO 구현 계획을 세운 자료”로 표현하는 것이 안전합니다.

## 무엇이 바뀌었는가

처음에는 세 프로젝트가 한 포트폴리오 저장소에 함께 있었습니다. 이후 저장소는 GitHub Pages 배포와 문서 공개에 맞춰 산출물 중심으로 정리되었고, 서버 런타임은 별도 구현 저장소와 연결되는 방향으로 재구성되었습니다.

이후 ServerCore 설명은 게임 서버 기능 자체보다 아래에 있는 네트워크 I/O 실행 기반으로 이동합니다. 현재 문서의 1장은 이 전환을 명시합니다.

> 처음에는 게임 서버를 만들기 위한 기반 코드로 시작했지만, TCP 연결 수락, 수신/송신, 시간 초과와 취소, 버퍼 소유권, 세션 수명주기, 스레드 간 작업 전달이 특정 게임 로직에만 종속되는 기능이 아니라고 정리했습니다.

## 확인 명령

```powershell
git show --stat 85c8f49
git show --stat 13f2411
git show 85c8f49:README.md
git show 85c8f49:01_ServerCore_v4/ServerCorePortfolio.qmd
git show 85c8f49:01_ServerCore_v4/Benchmarks/v4-benchmark-report.md
git show 85c8f49:03_FactionClash_Client/ClientPortfolio.qmd
git show 85c8f49:03_FactionClash_Client/proto/battle.proto
git show 85c8f49:02_SwarmDominion_DX11/SwarmDominion_DX11_Architecture.md
```
