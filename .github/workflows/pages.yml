name: Build and deploy docs site

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write
  pages: write
  id-token: write

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
  IMAGE_NAME: ghcr.io/mint-cocoa/mintcocoa-docs

concurrency:
  group: docs-site
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Install hub dependencies
        run: npm ci --prefix hub

      - name: Build unified Pages site
        run: bash scripts/build-site.sh

      - name: Verify ServerCore generated portfolio
        run: |
          set -euo pipefail
          test -f _site/portfolio/server/ServerCorePortfolio.html
          test -f _site/portfolio/servercore/index.html
          test -f _site/portfolio/servercore/docs/1.overview.html
          test -f _site/portfolio/servercore/docs/13.whole-repo-inventory.html
          test -f _site/portfolio/servercore/downloads/ServerCore-Portfolio-jinhoo.pdf
          grep -q "servercore/docs/1.overview.html" _site/portfolio/server/ServerCorePortfolio.html
          if find _site/portfolio/servercore -type f \( -name '*.qmd' -o -name '*.md' \) | grep -q .; then
            echo "ServerCore source markdown leaked into _site" >&2
            exit 1
          fi

      - name: Set up QEMU
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/setup-buildx-action@v3

      - name: Login to GHCR
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push docs image
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: .
          platforms: linux/amd64,linux/arm64
          push: true
          tags: |
            ${{ env.IMAGE_NAME }}:${{ github.sha }}
            ${{ env.IMAGE_NAME }}:main

      - name: Checkout ops repo
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: actions/checkout@v4
        with:
          repository: mint-cocoa/mintcocoa-ops
          token: ${{ secrets.OPS_REPO_TOKEN }}
          path: mintcocoa-ops

      - name: Promote docs image to OKE
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          set -euo pipefail
          file="mintcocoa-ops/apps/mintcocoa-docs/overlays/oke-prod/kustomization.yaml"
          if [ ! -f "$file" ]; then
            echo "GitOps overlay is not present yet; skipping promotion."
            exit 0
          fi
          sed -i "s|newName: .*|newName: ${IMAGE_NAME}|" "$file"
          sed -i "s|newTag: .*|newTag: ${GITHUB_SHA}|" "$file"
          cd mintcocoa-ops
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add apps/mintcocoa-docs/overlays/oke-prod/kustomization.yaml
          git diff --cached --quiet && exit 0
          git commit -m "Promote mintcocoa-docs to ${GITHUB_SHA}"
          git push

      - name: Configure Pages
        uses: actions/configure-pages@v5

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    runs-on: ubuntu-latest
    needs: build
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
